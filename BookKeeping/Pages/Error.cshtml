@page
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<div class="container mt-4">
    <div class="alert alert-danger" role="alert">
        <h1 class="h3 mb-3">系統暫時發生問題</h1>
        <p class="mb-2">抱歉，處理您的請求時發生未預期錯誤，請稍後再試。</p>
        <p class="mb-0">
            <a asp-page="/Index" class="btn btn-outline-danger btn-sm">返回首頁</a>
        </p>
    </div>

    @if (Model.ShowRequestId)
    {
        <p class="text-muted">
            <strong>Request ID:</strong> <code>@Model.RequestId</code>
        </p>
    }
</div>
